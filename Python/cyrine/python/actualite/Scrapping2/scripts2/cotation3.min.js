function RefreshPageFull() {
    location.href = location.href
}
var imgChart = document.getElementById("imgChart"), shortID = imgChart.getAttribute("data-name"), lang = {
    l_cours: "COURS",
    l_b: "B",
    l_m: "M",
    l_varperiod: "VARIATION PERIODE",
    l_msg3: "Il n'y a pas de cotations intraday pour cette valeur",
    l_msg4: "Pas de cotations disponibles pour cette valeur"
}, VGP = {
    urlWS: "/api/charting/",
    noAutoSaveTrace: !0,
    noAutoReadTrace: !0,
    drawings: [],
    noCommand: !0
}, graph = new RODGraph({
    callID: "imgChart",
    symbol: shortID
}), volSection = document.getElementById("vol"), isVolume = [], template;
volSection && (isVolume = volSection.textContent == "0" ? [] : ["volume"]);
template = {
    histoLength: 180,
    mainChart: {
        type: "line",
        typedef: {
            color: "rgba(160,160,160,1)",
            color2: "rgba(230,230,230,0.50)",
            color3: "rgba(230,230,230,0.50)",
            w1: 3
        }
    },
    indicators: isVolume,
    set: {
        nosticky: !0,
        fontSize: 12,
        reticColor: "#ea4e5a",
        nohighlow: !0
    }
};
graph.loadDefautChart(template);
imgChart.addEventListener("click", function() {
    return window.location.href = "/marches/graph/" + shortID,
    !1
});
tiptMe();
setInterval(RefreshPageFull, 15e4);
